{
  "name": "statskill-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Competency-based workforce upskilling platform for the Indian official statistical system (SIH prototype)",
  "type": "module",
  "workspaces": [
    "statskill-ai-backend",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev --workspace statskill-ai-backend",
    "dev:client": "npm run dev --workspace client",
    "dev:mongo": "node statskill-ai-backend/src/scripts/devDb.ts",
    "seed": "npm run seed --workspace statskill-ai-backend",
    "build": "npm run build --workspace client",
    "start": "npm run dev",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "9.1.2"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
